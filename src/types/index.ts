export type NavLink = {
  label: string;
  href: string;
};

export type ServiceCard = {
  id: string;
  title: string;
  description: string;
  icon: string;
  href: string;
  variant: "dark" | "lime" | "mint" | "sky";
};

export type PainPointCard = {
  id: string;
  title: string;
  description: string;
  icon: string;
  iconBgColor: string;
  iconColor: string;
};

export type TrustCompany = {
  id: string;
  name: string;
  icon: string;
};

export type SocialLink = {
  label: string;
  href: string;
  icon: string;
};

